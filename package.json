{
  "name": "discord-ci-status",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action: post a CI failure to Discord, delete it when the branch goes green",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
